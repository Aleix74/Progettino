<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="icon" type="image/x-icon" href="greenvisalogo.png"/>
    <link 
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
      rel="stylesheet" 
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
      crossorigin="anonymous"
      />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">      
    <link href="http://localhost:4000/style.css" rel="stylesheet">
</head>
<body>
    <% if (user) { %>
        <% if (user.nome_hotel) { %>
            <h1>Benvenuto <%= user.nome_hotel.charAt(0).toUpperCase() + user.nome_hotel.slice(1) %></h1>
            <input type="hidden" id="userType" value="<%= user.type %>">
        <% } else if (user.email) { %>
            <h1>Benvenuto <%= user.email %></h1>
        <% } %>
        <input type="hidden" id="userType" value="<%= user.type %>">
    <% } else { %>
        <h1>User not found</h1>
    <% } %>

    <a href="/users/logout" class="btn btn-success" style="margin-left: 0.5rem;">Logout</a>
    <a href="http://localhost:4000/progettino.html" class="btn btn-success" id="backButton" style="margin-left: 0.5rem;">Torna indietro</a>
</body>
</html> 


<!-- <a href="http://127.0.0.1:5500/progettino.html">Ritorna in home page</a> -->
<!-- <% if (user && user.nome_hotel) { %>
    <h1>Benvenuto <%= user.nome_hotel.charAt(0).toUpperCase() + user.nome_hotel.slice(1) %></h1>
<% } else { %>
    <h1>User not found</h1>
<% } %> -->


<!-- data-user-type="<%= user.type %>" -->

    <!-- <script>
        document.addEventListener("DOMContentLoaded", function() {
            // const userType = document.getElementById('userType').value;
            const userType = '<%= user.type %>';
            console.log("Saving userType to localStorage:", userType); // Debugging
            localStorage.setItem('userType', userType);

            // const backButton = document.getElementById('backButton');
            // backButton.addEventListener('click', function() {
            //     console.log(userType);
            //     window.location.href = 'http://127.0.0.1:5500/progettino.html';
            // });
            const backButton = document.getElementById('backButton');
            backButton.addEventListener('click', function() {
                console.log(userType);
                setTimeout(() => {
                    window.location.href = 'http://127.0.0.1:5500/progettino.html';
                }, 200); // Ritardo di 100 millisecondi per assicurarsi che localStorage sia aggiornato
            });
        });
    </script> -->

    <!-- <script>
        document.addEventListener("DOMContentLoaded", function() {
            const userType = '<%= user.type %>';
            console.log("Saving userType to localStorage:", userType); // Debugging
            localStorage.setItem('userType', userType);

            const backButton = document.getElementById('backButton');
            backButton.addEventListener('click', function() {
                console.log(userType);
                window.location.href = 'http://127.0.0.1:5500/progettino.html';
            });
        });
    </script> -->
    
    
    <!-- <script>
        document.addEventListener("DOMContentLoaded", function() {
            const backButton = document.getElementById('backButton');
            if (backButton) {
                const userType = backButton.getAttribute('data-user-type');
                console.log("Saving userType to localStorage:", userType); // Debugging
                if (userType) {
                    console.log("Saving userType to localStorage:", userType);  
                    localStorage.setItem('userType', userType);
                    console.log(localStorage.getItem('userType'));
                }
            } else {
                console.log("No backButton element found"); // Debugging
            }
        });
    </script> -->